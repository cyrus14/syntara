specVersion: "v1beta"
serviceId: "MEng Project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "MEng Project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
